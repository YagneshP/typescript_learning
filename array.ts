// if you want to define an array of numbers;

let numArray: number[] = [1,2,3];

// same with sting type

let strArray: string[] = ['a', 'b', 'c'];

// but [number] its different thing its called tuple;


// let arrNum: [number] = [1, 2] // get error [number, number] is not asssigned to [number]

//Another syntaxt T<U>

let numArray2: Array<number> = [1, 2, 3];