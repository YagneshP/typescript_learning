let unAssign: string;

// unAssign?.length; // Error : Variable 'unAssign' is used before being assigned.

// try this code

let unAssign_1: string | undefined;

unAssign_1?.length; // undefined is valid type
